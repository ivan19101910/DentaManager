@model IEnumerable<WebCoursework.Day>

@{
    ViewData["Title"] = "Index";
}

<h1>Дні</h1>

<p>
    <a asp-action="Create">Створити новий</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            @*<th>
                @Html.DisplayNameFor(model => model.CreatedDateTime)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.LastModifiedDateTime)
            </th>*@
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            @*<td>
                @Html.DisplayFor(modelItem => item.CreatedDateTime)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LastModifiedDateTime)
            </td>*@
            <td>
                <a asp-action="Edit" asp-route-id="@item.DayId">Редагувати</a> |
                @*<a asp-action="Details" asp-route-id="@item.DayId">Details</a> |*@
                <a asp-action="Delete" asp-route-id="@item.DayId">Видалити</a>
            </td>
        </tr>
}
    </tbody>
</table>
